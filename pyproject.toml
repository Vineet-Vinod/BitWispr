[project]
name = "bitwispr"
version = "0.1.0"
description = "Voice-to-text client with Trillim server (LLM + Whisper)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sounddevice",
    "numpy",
    "pynput",
    "evdev",
    "scipy",
    "trillim[voice]>=0.5.0",
]
