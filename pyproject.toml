[project]
name = "bitwispr"
version = "0.1.0"
description = "Voice-to-text using Whisper with real-time transcription"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper",
    "sounddevice",
    "numpy",
    "pynput",
    "evdev",
    "scipy",
]
